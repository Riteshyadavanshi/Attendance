FROM python:3.11-slim

WORKDIR /app

COPY ai/requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY ai/ .

EXPOSE 8001
CMD ["uvicorn", "face_service.main:app", "--host", "0.0.0.0", "--port", "8001"]
